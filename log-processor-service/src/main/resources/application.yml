spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/log_intelligence
    username: jeel
    password: jeel123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

# ✅ Custom Retry Config
app:
  retry:
    max-attempts: 3
